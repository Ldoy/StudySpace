//: [Previous](@previous)

import Foundation


//1. node 만들기 -> 왜 클래스 사용?
class Node {
    var next: Int?
    var value: Int?
    
    inif(next: next, value, value) {
        self.next = next
        self.value = value
    }
}

//2. Node에서 enqueue, dequeue, isempty, Clear, Peek 구현하기

class LinkedList {
    
    func enqueue(value: Int) {
        
    }
}


//: [Next](@next)
